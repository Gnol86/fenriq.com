import CharroiSubmissionsPage from "@project/features/charroi/submissions-page";

export default function Page({ searchParams }) {
    return <CharroiSubmissionsPage searchParams={searchParams} />;
}
